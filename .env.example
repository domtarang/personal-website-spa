# Development usually works with the Vite proxy in vite.config.js
VITE_API_BASE_URL=/api

# Example production value when the API is hosted on a subdomain.
# Change this if you deploy the API somewhere else.
# VITE_API_BASE_URL=https://api.domtarang.com/api
